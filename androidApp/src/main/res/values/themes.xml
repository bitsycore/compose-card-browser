<?xml version="1.0" encoding="utf-8"?>
<resources>

	<!--
	A bare splash theme. The app's real colours are Compose's, set in CardBrowserTheme; this exists
	only so the window between launch and the first frame is not the platform default.
	-->
	<style name="Theme.CardBrowser" parent="android:Theme.Material.NoActionBar">
		<item name="android:statusBarColor">@android:color/transparent</item>
		<item name="android:navigationBarColor">@android:color/transparent</item>
		<item name="android:windowBackground">@android:color/background_light</item>
	</style>

</resources>
